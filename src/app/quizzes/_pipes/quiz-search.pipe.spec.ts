import { QuizSearchPipe } from './quiz-search.pipe';

describe('QuizSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new QuizSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
