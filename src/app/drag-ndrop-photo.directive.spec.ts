import { DragNDropPhotoDirective } from './drag-ndrop-photo.directive';

describe('DragNDropPhotoDirective', () => {
  it('should create an instance', () => {
    const directive = new DragNDropPhotoDirective();
    expect(directive).toBeTruthy();
  });
});
