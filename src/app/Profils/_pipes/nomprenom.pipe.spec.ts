import { NomprenomPipe } from './subDirectory/nomprenom.pipe';

describe('NomprenomPipe', () => {
  it('create an instance', () => {
    const pipe = new NomprenomPipe();
    expect(pipe).toBeTruthy();
  });
});
